<!DOCTYPE html>
<html lang="es">
<title>Kylina jewels</title>
<!-- Include Head -->
<%- include('./partials/head') %>

	<body>

		<!-- Header -->
		<%- include('./partials/headerLogueado') %>
			<!-- /Header -->
			<!-- product detail -->
			<main class="product-detail">
				<div class="product-header">
					<h1 class="product-title"><%= producto.nombre_producto %></h1>
					<img src="<%= producto.imagen_producto %>" alt="<%= producto.nombre_producto%>" class="product-image">
					<a href="/users/profile/<%= cargado.id  %>"> Cargado por: <%= cargado.email %></a>
				</div>
				<div class="product-info">
					<p class="product-description"><strong>Descripción:</strong> <%= producto.descripcion %></p>
				</div>
				<div class="product-comments">
					<h2 class="comments-title">Comentarios</h2>
					<ul class="comments-list">
						<% for (let i = 0; i < producto.comentarios.length; i++) { %>
							<li class="comentarioDetail">
								<img src="<%= producto.comentarios[i].usuario.foto_perfil %>" alt="Foto de <%= producto.comentarios[i].usuario.email %>" class="comentario-img">
								<div class="comentario-content">
									<strong class="comentario-usuario"><%= producto.comentarios[i].usuario.email %></strong>
									<p class="comentario-texto"><%= producto.comentarios[i].comentario %></p>
								</div>
							</li>
						<% } %>
					</ul>
				</div>
			</main>
			
			<!-- /product detail -->

			<!-- footer -->
			<%- include('./partials/footer') %>
			<!-- /footer -->

	</body>

</html>

<!-- // dentro los tags, que imprimiran lo que retorne el metodo, incluimos a las vistas los partials mediante el metodo include (), que recibe un parametro strign que sera la ruta del archivo que queremos incluir  -->

